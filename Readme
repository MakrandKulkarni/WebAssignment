Read me

Steps to run the application
1) Create Schema in MySQL database
2) Change the database login and password and database url properties in application.properties file.
3) Run createtable.sql
